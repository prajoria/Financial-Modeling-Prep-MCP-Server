version: 1
runtime: "container"  # or "container"
startCommand:
  type: http
  # config implemented through smithery sdk `src/server/FmpMcpServer.ts`
build:
  dockerfile: "docker/Dockerfile"
  dockerBuildPath: "."